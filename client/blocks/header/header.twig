{% set deps = getClientFiles('header') %}

<div class="header">
    <div class="header__bar header__bar_horizontal">
        {% include deps.views.logo with {block: {
            mix: ['header__logo']
        }} %}
        
        <div class="header__icon-burger">
            <span class="icon-burger"></span>
        </div>
    </div>
</div>